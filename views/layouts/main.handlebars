<!DOCTYPE html>
<html lang="en">
<h1>Eat-da-Burger App</h1>

<p>Manage quotes with this app</p>
<!-- displaying burger names with a delete button-->
<ul>
  {{#each burgers}}
  	<li>
		<p><span class="label">ID:</span> {{this.id}}</p>
      	<p><span class="label">Name:</span> {{this.burger_name}}</p>
      	<!-- <p><span class="label">Author:</span> {{this.author}}</p> -->
<!-- want a put here, not delete -->
      	<form action="/delete?_method=DELETE" method="POST">
        	<input type="hidden" name="id" value="{{this.id}}">
        	<button type="submit">Devour!</button>
      	</form>

  	</li>
  {{/each}}
</ul>

<h1>Update the quote with an id of {{this.id}}</h1>

<form class="create-update-form" action="/update/{{this.id}}?_method=PUT" method="POST">

  <div class="form-group">
    <label for="auth">Author:</label>
    <input type="text" id="auth" name="author" value="{{this.author}}">
  </div>

  <div class="form-group">
    <label for="quo">Quote:</label>
    <textarea id="quo" name="quote" rows="8" cols="40">{{this.quote}}</textarea>
  </div>

  <button type="submit">Update Quote</button>
</form>










<!-- add a burger section -->
<h2>Add a burger</h2>

<form class="create-form" action="/create" method="POST">

  <div class="form-group">
    <label for="burg">Burger:</label>
    <input type="text" id="burg" name="burger">
  </div>

  <button type="submit">Add Burger</button>
</form>

<p>Enter a burger you want to eat and click the "Submit"</p>
</HTML>